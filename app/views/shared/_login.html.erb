
<div class="nav navbar-form navbar-right">
	<% if current_user %>
		<span style="color:grey">Signed in as <strong><%= current_user.name %></strong></span>&nbsp; 
		<%= link_to "Logout", signout_path, id: "sign_out", class: "btn btn-default btn-sm" %>
	<% else %>
		<%= link_to "Login", login_path, id: "sign_in", class: "btn btn-default btn-sm" %>
	<% end %>
</div>
